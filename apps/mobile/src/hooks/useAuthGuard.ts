// TEMPORARY: Demo-only auth bypass so we can view
// trip management and itinerary screens without a backend.
// Restore the original implementation after the demo.
export function useAuthGuard() {
  return true;
}
